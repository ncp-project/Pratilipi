module.exports = {
    API: {
        providers: [
            {
                key: "localhost",
                endpoint: 'http://localhost:8000/',
                token_prefix: 'JWT '
            }
        ]
    },
};